# Citation counts can be cached here (keyed by `<scholar_userid>:<scholar_id>`).
# This file is typically populated by a script that scrapes Google Scholar.
# Leave empty until then — the bibliography layout handles missing entries.
papers: {}
